'use strict';

var assert = require('chai').assert;

describe('Inventories', function () {

        describe('Inventories Library', function () {
                it('Should create a new inventory using a fake product number', function () {});

                it('Should update the product that was just created', function () {});

                it('Should get all available inventories', function () {});

                it('Should get an inventory using a specific product id', function () {});

                it('Should get the inventory using a join from the product table', function () {});

                it('Should delete the newly created inventory', function () {});
        });
});
//# sourceMappingURL=inventories.js.map