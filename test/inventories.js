let assert = require('chai').assert;

describe('Inventories', () => {

    describe('Inventories Library', () => {
        it('Should create a new inventory using a fake product number', () => {

        });

        it('Should update the product that was just created', () => {

        });

        it('Should get all available inventories', () => {

        });

        it('Should get an inventory using a specific product id', () => {

        });

        it('Should get the inventory using a join from the product table', () => {

        });

        it('Should delete the newly created inventory', () => {

        });
    });
});